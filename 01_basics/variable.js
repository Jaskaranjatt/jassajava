const accountId=1234
var accountName="Jaskaran"
let accountCity="Jalandhar"
var accountState;
// if const is used we cant change value of variable but in var and let we can
// var and let are almost same but not use var because of its scope.
console.log(accountId)
console.table([accountCity,accountId,accountName,accountState])